#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@tarojs/webpack5-runner/node_modules/sass/sass.js" "$@"
  ret=$?
else 
  node  "$basedir/../@tarojs/webpack5-runner/node_modules/sass/sass.js" "$@"
  ret=$?
fi
exit $ret
