function omit(obj = {}, fields = []) {
    const shallow = Object.assign({}, obj);
    fields.forEach((key) => {
        delete shallow[key];
    });
    return shallow;
}

export { omit };
//# sourceMappingURL=lodash.js.map
